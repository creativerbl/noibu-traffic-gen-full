# Noibu Traffic Generator - .env.example


# --- Core traffic shaping ---
SESSIONS_PER_MINUTE=25
AVG_SESSION_MINUTES=3
MAX_CONCURRENCY=100
GLOBAL_QPS_CAP=6

# Allow checkout (BigCommerce TEST MODE ONLY). Never use real PII.
ALLOW_CHECKOUT=true
CHECKOUT_COMPLETE_RATE=0.3

# --- Site ---
ORIGIN=https://noibu.mybigcommerce.com

# --- Browsers & devices (comma-separated) ---
BROWSERS=chromium,webkit,firefox
DEVICES=iphone-14,desktop-chrome,android-pixel

# --- Locales & timezones (comma-separated) ---
LOCALES=en-US,en-CA,en-GB,fr-CA
TIMEZONES=America/Toronto,America/New_York,America/Vancouver,Europe/London

# --- Flows (optional override; comma-separated paths) ---
# FLOWS=trafficgen/flows/category-browse.yaml,trafficgen/flows/checkout-complete.yaml


# --- BigCommerce TEST card ONLY ---
BC_TEST_CARD_NUMBER=4111111111111111
BC_TEST_EXP=12/30
BC_TEST_CVV=123
BC_TEST_NAME=Test User

# --- Flags ---
DEBUG=0
SMOKE=0

# --- Referrer ---
# 1) Sources + weights (any numbers; your runner normalizes to 100)
REFERRER_SOURCES=direct,google,bing,yahoo,duckduckgo,facebook,instagram,tiktok,linkedin,reddit
REFERRER_WEIGHTS=10,35,5,5,5,15,13,12,5,5

# 2) Campaign: make all *non-direct* sessions use this by default
UTM_CAMPAIGN_DEFAULT=trafficgen

# 3) Mediums:
UTM_MEDIUM_DEFAULT=paid-social
REFERRER_UTM_MEDIUMS=google:organic,bing:organic,yahoo:organic,duckduckgo:organic

# Device mix (optional; falls back to defaults if omitted)
DEVICE_MIX=iphone-safari:15,iphone-chrome:10,android-chrome:20,desktop-chrome:25,desktop-edge:10,desktop-safari:10,desktop-firefox:10

# Navigation wait behavior
PAGE_WAIT_UNTIL=load            # load | domcontentloaded | networkidle
POST_NAV_SETTLE_MIN_MS=250      # random settle after goto (min)
POST_NAV_SETTLE_MAX_MS=900      # random settle after goto (max)

# Scrolling behavior
SCROLL_PROB=0.75                # 75% of pageviews scroll at all
SCROLL_DEPTH_MIN=0.35           # min fraction of page height
SCROLL_DEPTH_MAX=0.95           # max fraction of page height
SCROLL_STEPS_MIN=2              # 2..6 steps by default
SCROLL_STEPS_MAX=6

